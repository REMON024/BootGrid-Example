@model IEnumerable<BootGrid_Example.Models.Student>

@{
    ViewBag.Title = "GetStudent";
}

<h2>GetStudent</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table id="product_data" class="table">
    <thead>

        <tr>
            <th data-column-id="Name">
                Name
            </th>
            <th data-column-id="Address">
                Address
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody></tbody>
    

    

</table>

@section Scripts{
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/jquery-bootgrid/1.3.1/jquery.bootgrid.css" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.2.0/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-bootgrid/1.3.1/jquery.bootgrid.js"></script>
    <script>
        $(document).ready(function () {

            var productTable = $('#product_data').bootgrid({
                ajax: true,
                rowSelect: true,
                get: function () {
                    return {
                        id: "b0df282a-0d67-40e5-8558-c9e93b7befed"
                    };
                },
                url: "GetStudents",
                formatters: {
                    "commands": function (column, row) {
                        console.log(column, row)
                        
                    }
                },
                searchSettings: {
                    delay: 100,
                    characters: 3,
                    placeholder:'Nasir vai'
                }
               
            });
        })
    </script>
}
